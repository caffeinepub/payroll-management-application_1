{
  "frontend/src/App.tsx": "Main app component with QueryClientProvider (staleTime:0, refetchOnMount:true), ThemeProvider, tab-based navigation for all six pages plus the new DataRecoveryPage, and Toaster.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend and localStorage operations with staleTime:0 and refetchOnMount:true on all queries; includes a complete set of CRUD hooks for employees, workDays, payments, monthlyBankSalaries, leaveRecords, payroll, and changeHistory.",
  "frontend/src/components/Header.tsx": "Navigation header with desktop and mobile nav for seven pages including the new \"Ανάκτηση\" (Data Recovery) tab with a dashed border indicator, plus theme toggle.",
  "frontend/src/components/Footer.tsx": "Simple footer component with attribution and link to caffeine.ai.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time user profile setup requiring name input before accessing the application.",
  "frontend/src/pages/LoginPage.tsx": "Landing page with login button and feature overview cards showcasing the payroll system capabilities.",
  "frontend/src/pages/EmployeesPage.tsx": "Employee management page with search, employee cards, add/edit dialog, manual refresh button with spinner, and a helpful empty state message pointing users to the data recovery page.",
  "frontend/src/components/EmployeeDialog.tsx": "Employee form dialog with employee type selection (monthly/hourly), conditional salary fields based on type, comprehensive validation, enhanced status indicators, and streamlined save process.",
  "frontend/src/pages/CalendarPage.tsx": "Calendar page with individual and bulk tabs; uses `useGetEmployees` and passes no props to `DailyBulkEntryTable`.",
  "frontend/src/components/WorkDayDialog.tsx": "Dialog for entering or editing a single work day entry; fixed the `setWorkDay.mutateAsync` call to use the correct `{ employeeId, workDay: WorkDay }` signature (wrapping `date`, `normalHours`, `overtimeHours`, `isLeave` inside a `workDay` object), and updated imports to use `Employee` from `../backend` and `WorkDay` from `../hooks/useQueries`.",
  "frontend/src/pages/PayrollPage.tsx": "Payroll page showing monthly salary calculations per employee or all employees; updated to import `PayrollData` from `../types`, use `number` for employee IDs and month/year parameters.",
  "frontend/src/pages/LeavePage.tsx": "Leave management page displaying employee leave balances with inline editing; updated to import `Employee` from `../types` and use `number` instead of `bigint` for employee IDs throughout.",
  "frontend/src/index.css": "Global styles with OKLCH color system defining blue-purple theme for both light and dark modes with proper contrast ratios, including enhanced calendar component styles with two variants: calendar-enhanced (12x12 cells) and calendar-enhanced-large (16x16 cells with larger text and improved spacing) for better accessibility and visibility in the daily entry table.",
  "frontend/src/components/BulkEntryTable.tsx": "Bulk entry table component displaying all employees in rows with columns for each day of the month, allowing quick inline editing of normal hours and overtime hours with auto-save on blur, and checkbox toggles for marking leave days.",
  "frontend/src/components/DailyBulkEntryTable.tsx": "Self-contained daily bulk work hours entry table with built-in date selector (day/month/year dropdowns), uses `useGetEmployees`, `useGetDailyWorkEntries`, and `useSetWorkDaysBulk` hooks.",
  "frontend/src/pages/PaymentsPage.tsx": "Payments page with bulk entry and monthly view tabs, month/year selectors, and a new payment dialog; fixed by replacing the forbidden `require()` call in `MonthlyPaymentsView` with proper ES module imports at the top of the file.",
  "frontend/src/components/MonthlyBankSalaryDialog.tsx": "Dialog for adding an individual monthly bank salary entry, updated to pass `number` values (not `bigint`) to the `useSetMonthlyBankSalary` hook.",
  "frontend/src/components/MonthlyBankSalaryManager.tsx": "Per-employee monthly bank salary card allowing entry and saving of a bank salary amount; fixed by replacing the non-existent `useGetMonthlyBankSalary` with `useGetMonthlyBankSalaries` and filtering by `employeeId`.",
  "frontend/src/components/PaymentEditDialog.tsx": "Standalone dialog for adding or editing payment records with built-in employee selector, month/year/date fields, and uses `useAddPayment`/`useUpdatePayment` hooks directly.",
  "frontend/src/hooks/useInternetIdentity.ts": "Internet Identity provider with automatic delegation refresh (8-hour TTL), expiry detection, and silent re-login before delegation expires to prevent session interruptions.",
  "frontend/src/components/BulkPaymentEntryTable.tsx": "Self-contained bulk payment entry table with built-in month/year/date selectors, uses `useGetEmployees` and `useAddPaymentsBulk` with correct `{ entries }` signature.",
  "frontend/src/components/UnauthorizedAccessScreen.tsx": "Unauthorized access screen component displaying \"Μη εξουσιοδοτημένη πρόσβαση\" message with logout button for non-admin users attempting to access the application.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates an authenticated or anonymous backend actor; after initializing access control (which makes the first caller admin), it also assigns the current authenticated user the `user` role so all app functions work without \"Only admins can perform this action\" errors.",
  "frontend/src/components/LeaveDayDialog.tsx": "Dialog for adding a leave day for a single employee, updated to accept `employeeId` as `number` instead of `bigint` to match the `useToggleLeaveDay` hook.",
  "frontend/src/components/BulkLeaveDayDialog.tsx": "Dialog for adding a leave day to all employees simultaneously; fixed to fetch employees and pass `{ date, employeeIds }` to `useAddBulkLeaveDay` instead of a bare string.",
  "frontend/src/components/BulkMonthlyBankSalaryDialog.tsx": "Bulk monthly bank salary entry table for all employees; fixed to pass a plain array directly to `useSetMonthlyBankSalariesBulk` instead of wrapping it in `{ entries }`.",
  "frontend/src/pages/MonthlyBankSalariesPage.tsx": "Monthly bank salaries page with bulk/individual/view tabs; passes `month`, `year` props to `BulkMonthlyBankSalaryDialog` and only `employeeId`, `month`, `year` to `MonthlyBankSalaryManager`.",
  "frontend/src/components/MonthlyBankSalariesView.tsx": "Displays all monthly bank salaries grouped by employee; fixed to pass required `month` and `year` arguments to `useGetAllMonthlyBankSalaries`, use `Number(e.id)` for the employee map key, import types from `useQueries` instead of `types`, and removed the unused `onEdit` prop.",
  "frontend/src/components/MonthlyBankSalaryEditDialog.tsx": "Dialog component for editing existing monthly bank salary records with month/year selection and decimal amount input supporting Greek formatting.",
  "frontend/src/components/LeaveEditDialog.tsx": "Dialog displaying an employee's recorded leave days with delete functionality; fixed to use `LeaveDay` objects (with `.id` and `.date` properties) for keys, display, and deletion instead of treating them as strings.",
  "frontend/src/components/EmployeeFolderView.tsx": "Employee folder view dialog component displaying comprehensive employee details, contact information, and chronological change history with edit functionality.",
  "frontend/src/docs/employee-calendar-sync.md": "Internal developer documentation explaining the React Query cache invalidation strategy for bidirectional synchronization between Individual and Bulk work hour entry modes in the Employee Calendar feature.",
  "frontend/src/pages/DataRecoveryPage.tsx": "New data recovery page that exports/imports all localStorage data as JSON, shows current data status per key, and provides step-by-step instructions for recovering data from a previous deployment version.",
  "frontend/src/main.tsx": "Minimal React entry point that renders App without a duplicate QueryClientProvider (the provider is now only in App.tsx)."
}